#include "elementos.h"

using namespace std;

void main(int argc, char *argv[]) {
    /*int numEntrada = 5;
    int numPosc = 6;
    int numSalida = 10;
    int tamColasI = 6;

    switch (argv[1]) {
    case 'init':
      break;
    
    case 'reg':
      if (argc == 5) { // puede ser path, archivo o -
        reg(argv[4]);
      } else {
        reg(argv[2])
      }
      break;

    case 'ctrl':
      ctrl();
      break;

    case 'rep':
      if (argc == 6) {
        rep(argv[4],argv[5]);
      } else {
        rep(argv[2],argv[3]);
      }
      break;

    }*/
}
